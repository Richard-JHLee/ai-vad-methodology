# VAD example: Python

Placeholder for applying VAD to an AI-assisted Python change (for example:
FastAPI/Flask endpoint + service + pytest).

## Suggested contents (to add)

* Requirement contract for an API or data-processing change  
* System map of entrypoint → domain → persistence  
* Change map and verification with pytest + type checking  

## Status

Draft scaffold — contributions welcome via pull request.
