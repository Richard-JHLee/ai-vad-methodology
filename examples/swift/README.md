# VAD example: Swift / SwiftUI

Placeholder for applying VAD to an AI-assisted iOS change (for example: a new
screen + ViewModel + XCTest).

## Suggested contents (to add)

* Requirement contract for UI + networking behavior  
* System map of `View → ViewModel → Service → API`  
* Change map limited to a few Swift files  
* Verification with XCTest / UI checks and rollback notes (feature flag)

## Status

Draft scaffold — contributions welcome via pull request.
